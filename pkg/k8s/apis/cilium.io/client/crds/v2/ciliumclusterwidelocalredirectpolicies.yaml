---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ciliumclusterwidelocalredirectpolicies.cilium.io
spec:
  group: cilium.io
  names:
    categories:
    - cilium
    - ciliumpolicy
    kind: CiliumClusterwideLocalRedirectPolicy
    listKind: CiliumClusterwideLocalRedirectPolicyList
    plural: ciliumclusterwidelocalredirectpolicies
    shortNames:
    - cclrp
    singular: ciliumclusterwidelocalredirectpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: |-
          CiliumClusterwideLocalRedirectPolicy is a Kubernetes custom resource that
          contains a specification to redirect traffic locally within a node.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the desired behavior of the cluster-wide local redirect
              policy.
            properties:
              addressMatcher:
                description: AddressMatcher matches traffic destined to a single IP
                  address.
                properties:
                  ip:
                    description: |-
                      IP is the destination IP address for traffic to be redirected.
                      Both IPv4 and IPv6 addresses are supported.
                    format: ip
                    type: string
                required:
                - ip
                type: object
              localEndpointSelector:
                description: |-
                  LocalEndpointSelector selects node-local pod(s) where traffic is
                  redirected to.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          enum:
                          - In
                          - NotIn
                          - Exists
                          - DoesNotExist
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      description: MatchLabelsValue represents the value from the
                        MatchLabels {key,value} pair.
                      maxLength: 63
                      pattern: ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              ports:
                description: |-
                  Ports specifies the source port and protocol to match and the target port
                  on the selected local endpoint(s). When omitted with a serviceMatcher, the
                  Service's port-to-targetPort mappings are inherited.
                items:
                  description: |-
                    CiliumClusterwideLocalRedirectPolicyPort specifies a source port and its
                    target port along with the transport protocol.
                  properties:
                    port:
                      description: |-
                        Port is the destination port to match. The string is strictly parsed as
                        a single uint16.
                      pattern: ^()([1-9]|[1-5]?[0-9]{2,4}|6[1-4][0-9]{3}|65[1-4][0-9]{2}|655[1-2][0-9]|6553[0-5])$
                      type: string
                    protocol:
                      description: |-
                        Protocol is the L4 protocol.
                        Accepted values are TCP and UDP.
                      enum:
                      - TCP
                      - UDP
                      type: string
                    targetPort:
                      description: |-
                        TargetPort is the port on the selected local endpoint. The string is
                        strictly parsed as a single uint16.
                      pattern: ^()([1-9]|[1-5]?[0-9]{2,4}|6[1-4][0-9]{3}|65[1-4][0-9]{2}|655[1-2][0-9]|6553[0-5])$
                      type: string
                  required:
                  - port
                  - protocol
                  - targetPort
                  type: object
                type: array
              serviceMatcher:
                description: ServiceMatcher matches traffic destined to a Kubernetes
                  Service.
                properties:
                  namespace:
                    description: Namespace is the namespace of the destination Kubernetes
                      Service.
                    type: string
                  serviceName:
                    description: ServiceName is the name of the destination Kubernetes
                      Service.
                    type: string
                required:
                - namespace
                - serviceName
                type: object
            required:
            - localEndpointSelector
            type: object
            x-kubernetes-validations:
            - message: exactly one of addressMatcher or serviceMatcher must be specified
              rule: has(self.addressMatcher) != has(self.serviceMatcher)
            - message: ports must be specified with addressMatcher
              rule: has(self.serviceMatcher) || size(self.ports) > 0
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
